import "./AllDeleteButton.css"

const AllDeleteButton = (props) => {

    return(

        <button type="button" className="all-delete" onClick={props.xxxAll }> Vymazat vse </button>
    )
}

export default AllDeleteButton